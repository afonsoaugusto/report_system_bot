language: python

python:
  - "3.6"

before_install:
  - sudo apt-get update
  - sudo apt-get install -y realpath

sudo: required

install:
  - pip install -r requirements.txt
  - pip install coveralls

before_script: . scripts_teste/export

script:
  - nosetests --with-coverage

after_success:
  - coveralls
